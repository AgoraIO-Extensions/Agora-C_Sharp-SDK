#region Generated by `terra/node/src/rtc/impl/renderers.ts`. DO NOT MODIFY BY HAND.
#endregion

#define AGORA_RTC
#define AGORA_RTM
using System;
using view_t = System.UInt64;

namespace Agora.Rtc 
{
    public partial class {{{user_data.implNameString}}}
    {

        {{#methods}}
        {{^user_data.isHide}}
        {{#user_data.macro}} 
        #if {{user_data.macro}}
        {{/user_data.macro}}
        public {{user_data.returnTypeString}} {{user_data.nameString}} (int playerId{{#user_data.formalParameterString}}, {{{user_data.formalParameterString}}}{{/user_data.formalParameterString}})
        {
            _param.Clear();
            _param.Add("playerId", playerId);
            {{#user_data.parameterAddToJsonString}}
            {{{.}}}
            {{/user_data.parameterAddToJsonString}}

            var json = AgoraJson.ToJson(_param);
            var nRet = AgoraRtcNative.CallIrisApiWithArgs(_irisApiEngine, AgoraApiType.{{{user_data.IrisApiIdParser.key}}},
                json, (UInt32)json.Length,
                IntPtr.Zero, 0,
                ref _apiParam);

            {{{user_data.returnValueGetFromString}}};

            {{#user_data.parameterGetFromJsonGetString}}
            if (nRet == 0)
            {
                {{{.}}}
            }
            {{/user_data.parameterGetFromJsonGetString}}

            return result;
        }
        {{#user_data.macro}} 
        #endif
        {{/user_data.macro}}
        
        {{/user_data.isHide}}
        {{/methods}}
    }
}