#region Generated by `terra/node/src/rtc/interface/renderers.ts`. DO NOT MODIFY BY HAND.
#endregion

#define AGORA_RTC
#define AGORA_RTM
using System;
using view_t = System.UInt64;

namespace Agora.Rtc {
    public partial class {{user_data.middleNameString}} : {{name}}
    {
        {{#methods}}
        {{^user_data.isHide}}
        {{#user_data.macro}} 
        #if {{user_data.macro}}
        {{/user_data.macro}}
        public event Action{{#user_data.signatureParameterString}}<{{user_data.signatureParameterString}}>{{/user_data.signatureParameterString}} Event{{user_data.nameString}}{{user_data.repeat}};

        public override void {{user_data.nameString}}({{user_data.formalParameterString}})
        { 
            if(Event{{user_data.nameString}}{{user_data.repeat}} == null) return;
            Event{{user_data.nameString}}{{user_data.repeat}}.Invoke({{user_data.actualParameterString}});
        }
        {{#user_data.macro}} 
        #endif
        {{/user_data.macro}}

        {{/user_data.isHide}}
        {{/methods}}
    }
}